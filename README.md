# Portfólio

Site pessoal com os meus projetos. HTML e CSS, sem framework e sem JavaScript.

Os prints em `img/` são capturas dos projetos publicados.

Headers de segurança (CSP sem script, HSTS, `X-Frame-Options`) ficam no `vercel.json`.

Pra ver local:

```bash
python -m http.server 8000
```
